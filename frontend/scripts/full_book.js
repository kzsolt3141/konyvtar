const bid = document.getElementById("bid").getAttribute("content");
console.log("generate the full list based on:" + bid);

//TODO: fill the HTML with data
// metadata on Page: the BOOK data (ID, Title, Author ....)
// metadata on Page: notes from that book (Date, Notes)
// metadata on Page: loans for that book (Date, IN/OUT, User, Notes)
